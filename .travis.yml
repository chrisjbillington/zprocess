language: python

# Install the codecov pip dependency
install:
  - pip install -r requirements.txt
  - pip install codecov

# Run the unit test
script:
  - python run_tests.py

# Push the results back to codecov
after_success:
  - coverage combine
  - codecov